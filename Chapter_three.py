
#   To check the reference or address or ID of a variable or value.
id(1)
print



st = 'hello'
st.find('o')


new_name = st.find('Olaifa')

num = 1/3
print('result of 1/3 = {}'.format(num))
print('result of 1/3 = {:<4}'.format(num))
print('result of 1/3 = {:<4.2f}'.format(num))



#   To  print both index and element of a list, we use the keyword ENUMERATE
li = [99, 23,4,2,3,4,5,56,7,3]
for index, element in enumerate(li):
    print(index, element)

    for index, element in enumerate(li):
        print ('the element {:2d} is at index {:2d}'.format (element, index)


               river = 'Mississippi'
               target = input('Input a character to find: ')
               for index, letter in enumerate(river):
               print(index, char)



               
               if letter == target:
               print("Letter found at index: ", index)
               


